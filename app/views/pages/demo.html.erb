<% content_for :title, "How Our Outbound Machine Works | Ze Lu Sottomayor" %>
<% content_for :meta_description, "We scrape the internet daily for your exact buyers, write personalised messages, and run outreach at scale — so your pipeline keeps moving without an SDR team." %>

<style>
  /* ── DEMO PAGE ─────────────────────────────────────── */
  .demo-page-hero {
    padding: 80px 0 64px;
    text-align: center;
    background: var(--gradient-hero);
  }

  .demo-eyebrow {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-primary);
    background: rgba(39, 90, 78, 0.08);
    border: 1px solid rgba(39, 90, 78, 0.2);
    padding: 5px 16px;
    border-radius: var(--radius-full);
    margin-bottom: 28px;
  }

  .demo-headline {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.03em;
    line-height: 1.1;
    color: var(--color-text-primary);
    margin-bottom: 20px;
  }

  .demo-headline em {
    font-style: normal;
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .demo-sub {
    font-family: var(--font-sans);
    font-size: var(--font-size-lg);
    color: var(--color-text-muted);
    max-width: 560px;
    margin: 0 auto 40px;
    line-height: var(--line-height-relaxed);
  }

  .demo-cta-group {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ── VIDEO SECTION ─────────────────────────────────── */
  .demo-video-section {
    padding: 48px 0 64px;
  }

  .demo-video-container {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    border: 1px solid var(--color-border);
    box-shadow: var(--shadow-xl);
    background: var(--color-surface);
    aspect-ratio: 16/9;
  }

  .demo-video-placeholder {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 20px;
    background: linear-gradient(135deg, var(--color-sand-100) 0%, var(--color-sand-50) 100%);
  }

  .demo-play-btn {
    width: 80px;
    height: 80px;
    background: var(--color-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all var(--transition-base);
    box-shadow: 0 8px 24px rgba(39, 90, 78, 0.3);
  }

  .demo-play-btn:hover {
    background: var(--color-primary-hover);
    transform: scale(1.06);
    box-shadow: 0 12px 32px rgba(39, 90, 78, 0.4);
  }

  .demo-play-btn svg {
    width: 30px;
    height: 30px;
    fill: #fff;
    margin-left: 5px;
  }

  .demo-video-label {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  .demo-video-label strong { color: var(--color-text-secondary); }

  /* ── DIFFERENTIATOR CALLOUT ────────────────────────── */
  .demo-diff-section {
    padding: 0 0 64px;
  }

  .demo-diff-box {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-left: 4px solid var(--color-primary);
    border-radius: var(--radius-xl);
    padding: 32px 40px;
    display: flex;
    gap: 28px;
    align-items: flex-start;
  }

  .demo-diff-icon {
    font-size: 32px;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .demo-diff-title {
    font-family: var(--font-display);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: 8px;
    letter-spacing: -0.02em;
  }

  .demo-diff-body {
    font-family: var(--font-sans);
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  .demo-diff-body strong {
    color: var(--color-text-primary);
    font-weight: var(--font-weight-semibold);
  }

  /* ── PROCESS SECTION ───────────────────────────────── */
  .demo-process-section {
    padding: 64px 0;
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }

  .demo-section-label {
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-text-muted);
    text-align: center;
    margin-bottom: 48px;
  }

  .demo-flow {
    display: flex;
    gap: 0;
    align-items: stretch;
  }

  .demo-step {
    flex: 1;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: 32px 24px;
    text-align: center;
    position: relative;
    background: var(--color-surface);
    transition: border-color var(--transition-base), transform var(--transition-base), box-shadow var(--transition-base);
  }

  .demo-step:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .demo-step + .demo-step {
    margin-left: 12px;
  }

  .demo-step--result {
    border-color: var(--color-accent);
    background: linear-gradient(135deg, rgba(200, 161, 90, 0.04) 0%, var(--color-surface) 100%);
  }

  .demo-step--result:hover {
    border-color: var(--color-gold-600);
  }

  .demo-step-num {
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 16px;
  }

  .demo-step--result .demo-step-num {
    color: var(--color-accent);
  }

  .demo-step-icon {
    font-size: 28px;
    margin-bottom: 14px;
    display: block;
  }

  .demo-step-title {
    font-family: var(--font-display);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    letter-spacing: -0.01em;
    margin-bottom: 10px;
  }

  .demo-step--result .demo-step-title {
    color: var(--color-gold-600);
  }

  .demo-step-desc {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    line-height: var(--line-height-relaxed);
  }

  .demo-step-arrow {
    position: absolute;
    right: -18px;
    top: 50%;
    transform: translateY(-50%);
    width: 24px;
    height: 24px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
    font-size: 10px;
    color: var(--color-text-muted);
  }

  /* ── WHAT YOU GET ───────────────────────────────────── */
  .demo-deliverables-section {
    padding: 64px 0;
  }

  .demo-deliverables-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .demo-deliverable-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: 28px 24px;
    transition: border-color var(--transition-base), box-shadow var(--transition-base);
  }

  .demo-deliverable-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-md);
  }

  .demo-deliverable-icon {
    font-size: 24px;
    margin-bottom: 14px;
    display: block;
  }

  .demo-deliverable-title {
    font-family: var(--font-display);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: 8px;
    letter-spacing: -0.01em;
  }

  .demo-deliverable-desc {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  /* ── PROOF STRIP ───────────────────────────────────── */
  .demo-proof-section {
    padding: 0 0 64px;
  }

  .demo-proof-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--color-border);
    border-radius: var(--radius-xl);
    overflow: hidden;
    border: 1px solid var(--color-border);
  }

  .demo-proof-cell {
    background: var(--color-surface);
    padding: 36px 24px;
    text-align: center;
  }

  .demo-proof-num {
    font-family: var(--font-display);
    font-size: 3rem;
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.04em;
    color: var(--color-text-primary);
    margin-bottom: 6px;
    line-height: 1;
  }

  .demo-proof-num em {
    font-style: normal;
    color: var(--color-primary);
  }

  .demo-proof-label {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    font-weight: var(--font-weight-medium);
  }

  /* ── HONEST SECTION ────────────────────────────────── */
  .demo-honest-section {
    padding: 0 0 64px;
  }

  .demo-honest-box {
    background: linear-gradient(135deg, var(--color-ink-900) 0%, var(--color-ink-700) 100%);
    border-radius: var(--radius-xl);
    padding: 56px 48px;
    text-align: center;
  }

  .demo-honest-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent);
    background: rgba(200, 161, 90, 0.15);
    border: 1px solid rgba(200, 161, 90, 0.3);
    padding: 5px 16px;
    border-radius: var(--radius-full);
    margin-bottom: 24px;
  }

  .demo-honest-headline {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.02em;
    color: #fff;
    margin-bottom: 16px;
  }

  .demo-honest-sub {
    font-family: var(--font-sans);
    font-size: var(--font-size-base);
    color: rgba(255, 255, 255, 0.55);
    max-width: 560px;
    margin: 0 auto;
    line-height: var(--line-height-relaxed);
  }

  /* ── CTA FOOTER ────────────────────────────────────── */
  .demo-cta-section {
    padding: 64px 0 96px;
    text-align: center;
  }

  .demo-cta-note {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin-top: 16px;
  }

  /* ── RESPONSIVE ────────────────────────────────────── */
  @media (max-width: 900px) {
    .demo-deliverables-grid { grid-template-columns: 1fr 1fr; }
  }

  @media (max-width: 760px) {
    .demo-page-hero { padding: 56px 0 48px; }
    .demo-flow { flex-direction: column; gap: 12px; }
    .demo-step + .demo-step { margin-left: 0; }
    .demo-step-arrow { display: none; }
    .demo-proof-grid { grid-template-columns: 1fr; }
    .demo-honest-box { padding: 36px 24px; }
    .demo-diff-box { flex-direction: column; gap: 16px; padding: 24px; }
    .demo-deliverables-grid { grid-template-columns: 1fr; }
  }
</style>

<!-- HERO -->
<section class="demo-page-hero">
  <div class="container container--narrow">
    <span class="demo-eyebrow">How it works</span>
    <h1 class="demo-headline">
      We build and run your<br>
      <em>outbound machine</em> — you focus on closing
    </h1>
    <p class="demo-sub">
      We scrape the internet daily for your exact buyers, write personalised messages, and send them at scale. No SDR. No static lists. No fluff.
    </p>
    <div class="demo-cta-group">
      <%= link_to book_call_path, class: "cta-button cta-button--primary cta-button--large" do %>
        Book a 20-min call →
      <% end %>
      <%= link_to "#process", class: "cta-button cta-button--secondary" do %>
        ↓ See how it works
      <% end %>
    </div>
  </div>
</section>

<!-- VIDEO -->
<section class="demo-video-section">
  <div class="container container--narrow">
    <div class="demo-video-container">
      <div class="demo-video-placeholder">
        <div class="demo-play-btn">
          <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><polygon points="5,3 19,12 5,21" /></svg>
        </div>
        <p class="demo-video-label"><strong>90-second walkthrough</strong> — coming soon</p>
      </div>
    </div>
  </div>
</section>

<!-- DIFFERENTIATOR -->
<section class="demo-diff-section">
  <div class="container container--narrow">
    <div class="demo-diff-box">
      <div class="demo-diff-icon">🔎</div>
      <div>
        <div class="demo-diff-title">We don't buy lists. We scrape them fresh, every day.</div>
        <p class="demo-diff-body">
          Most agencies pull from Apollo or ZoomInfo — the same stale databases everyone else is hammering.
          We scrape the web daily for companies that match your exact ICP, then drill down to the right individuals.
          <strong>Fresh leads, new every day, for as long as we're running.</strong>
        </p>
      </div>
    </div>
  </div>
</section>

<!-- PROCESS FLOW -->
<section class="demo-process-section" id="process">
  <div class="container">
    <p class="demo-section-label">The system — how it works</p>
    <div class="demo-flow">

      <div class="demo-step">
        <div class="demo-step-num">Step 01</div>
        <span class="demo-step-icon">🎯</span>
        <div class="demo-step-title">Define your ICP</div>
        <div class="demo-step-desc">We work with you to nail exactly who to target — industry, size, location, role. Precise from day one.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step">
        <div class="demo-step-num">Step 02</div>
        <span class="demo-step-icon">🕸️</span>
        <div class="demo-step-title">Daily web scraping</div>
        <div class="demo-step-desc">Our machine scrapes the internet every day for companies matching your ICP. Fresh leads, not recycled databases.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step">
        <div class="demo-step-num">Step 03</div>
        <span class="demo-step-icon">✍️</span>
        <div class="demo-step-title">Personalise at scale</div>
        <div class="demo-step-desc">Every message is written to the specific company. Two lines, relevant, no fluff. Not a template blast.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step">
        <div class="demo-step-num">Step 04</div>
        <span class="demo-step-icon">📤</span>
        <div class="demo-step-title">Send & track</div>
        <div class="demo-step-desc">We run the sequences and feed results directly into your CRM. You get notified on replies — we handle follow-ups.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step demo-step--result">
        <div class="demo-step-num">Ongoing</div>
        <span class="demo-step-icon">♻️</span>
        <div class="demo-step-title">It keeps running</div>
        <div class="demo-step-desc">New leads scraped daily, new messages sent daily. This isn't a campaign. It's a machine that runs continuously.</div>
      </div>

    </div>
  </div>
</section>

<!-- WHAT YOU GET -->
<section class="demo-deliverables-section">
  <div class="container container--narrow">
    <p class="demo-section-label">What you get</p>
    <div class="demo-deliverables-grid">

      <div class="demo-deliverable-card">
        <span class="demo-deliverable-icon">📋</span>
        <div class="demo-deliverable-title">Fresh leads, daily</div>
        <p class="demo-deliverable-desc">New companies and contacts added every day, scraped from the web to match your ICP. Never the same list twice.</p>
      </div>

      <div class="demo-deliverable-card">
        <span class="demo-deliverable-icon">📨</span>
        <div class="demo-deliverable-title">High-volume outreach</div>
        <p class="demo-deliverable-desc">Up to 150 personalised emails per day, per inbox. Multiple inboxes available for higher volume.</p>
      </div>

      <div class="demo-deliverable-card">
        <span class="demo-deliverable-icon">📊</span>
        <div class="demo-deliverable-title">Campaign dashboard</div>
        <p class="demo-deliverable-desc">Real-time visibility into emails sent, open rates, replies, and pipeline movement. You see exactly what's happening.</p>
      </div>

      <div class="demo-deliverable-card">
        <span class="demo-deliverable-icon">🔗</span>
        <div class="demo-deliverable-title">CRM integration</div>
        <p class="demo-deliverable-desc">Works with whatever you already use — HubSpot, Salesforce, Notion, Google Sheets. No forced migration.</p>
      </div>

      <div class="demo-deliverable-card">
        <span class="demo-deliverable-icon">✉️</span>
        <div class="demo-deliverable-title">Handled follow-ups</div>
        <p class="demo-deliverable-desc">The sequence runs automatically. Replies come to you. We manage the cadence so nothing falls through the cracks.</p>
      </div>

      <div class="demo-deliverable-card">
        <span class="demo-deliverable-icon">🧠</span>
        <div class="demo-deliverable-title">Built for your ICP</div>
        <p class="demo-deliverable-desc">Every client gets a system tuned to their exact target. Same machine, different config. Precision, not spray-and-pray.</p>
      </div>

    </div>
  </div>
</section>

<!-- PROOF STRIP -->
<section class="demo-proof-section">
  <div class="container container--narrow">
    <div class="demo-proof-grid">
      <div class="demo-proof-cell">
        <div class="demo-proof-num"><em>150</em></div>
        <div class="demo-proof-label">Emails sent per inbox, per day</div>
      </div>
      <div class="demo-proof-cell">
        <div class="demo-proof-num"><em>Daily</em></div>
        <div class="demo-proof-label">Fresh leads scraped from the web</div>
      </div>
      <div class="demo-proof-cell">
        <div class="demo-proof-num"><em>0</em></div>
        <div class="demo-proof-label">Stale list databases used</div>
      </div>
    </div>
  </div>
</section>

<!-- HONEST SECTION -->
<section class="demo-honest-section">
  <div class="container container--narrow">
    <div class="demo-honest-box">
      <span class="demo-honest-badge">What we control</span>
      <h2 class="demo-honest-headline">We promise volume and precision. Not magic.</h2>
      <p class="demo-honest-sub">
        We can't control whether your prospect replies — but we control everything before that moment. Fresh targeting, personalised messages, and consistent daily volume give your pipeline the best possible chance. That's what we're accountable for.
      </p>
    </div>
  </div>
</section>

<!-- CTA FOOTER -->
<section class="demo-cta-section">
  <div class="container container--narrow">
    <h2 style="font-family: var(--font-display); font-size: clamp(1.5rem, 3vw, 2rem); font-weight: var(--font-weight-extrabold); letter-spacing: -0.02em; color: var(--color-text-primary); margin-bottom: 12px;">
      Want to see if this fits your business?
    </h2>
    <p style="font-family: var(--font-sans); color: var(--color-text-muted); margin-bottom: 32px; max-width: 480px; margin-left: auto; margin-right: auto;">
      Book a 20-min call. I'll tell you exactly what ICP we'd target, how many leads we'd expect to find daily, and what volume looks like for your market.
    </p>
    <%= link_to book_call_path, class: "cta-button cta-button--primary cta-button--large" do %>
      Book a 20-min call →
    <% end %>
    <p class="demo-cta-note">No commitment. Just a conversation.</p>
  </div>
</section>
