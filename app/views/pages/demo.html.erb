<% content_for :title, "How We Book You 3–5 Qualified Calls in 30 Days | Ze Lu Sottomayor" %>
<% content_for :meta_description, "See exactly how our outbound system finds your ideal buyers, sends personalised messages at scale, and books qualified calls — without an SDR team." %>

<style>
  /* ── DEMO PAGE ─────────────────────────────────────── */
  .demo-page-hero {
    padding: 80px 0 64px;
    text-align: center;
    background: var(--gradient-hero);
  }

  .demo-eyebrow {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-primary);
    background: rgba(39, 90, 78, 0.08);
    border: 1px solid rgba(39, 90, 78, 0.2);
    padding: 5px 16px;
    border-radius: var(--radius-full);
    margin-bottom: 28px;
  }

  .demo-headline {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.03em;
    line-height: 1.1;
    color: var(--color-text-primary);
    margin-bottom: 20px;
  }

  .demo-headline em {
    font-style: normal;
    background: var(--gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .demo-sub {
    font-family: var(--font-sans);
    font-size: var(--font-size-lg);
    color: var(--color-text-muted);
    max-width: 520px;
    margin: 0 auto 40px;
    line-height: var(--line-height-relaxed);
  }

  .demo-cta-group {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ── VIDEO SECTION ─────────────────────────────────── */
  .demo-video-section {
    padding: 48px 0 64px;
  }

  .demo-video-container {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    border: 1px solid var(--color-border);
    box-shadow: var(--shadow-xl);
    background: var(--color-surface);
    aspect-ratio: 16/9;
  }

  .demo-video-placeholder {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 20px;
    background: linear-gradient(135deg, var(--color-sand-100) 0%, var(--color-sand-50) 100%);
  }

  .demo-play-btn {
    width: 80px;
    height: 80px;
    background: var(--color-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all var(--transition-base);
    box-shadow: 0 8px 24px rgba(39, 90, 78, 0.3);
  }

  .demo-play-btn:hover {
    background: var(--color-primary-hover);
    transform: scale(1.06);
    box-shadow: 0 12px 32px rgba(39, 90, 78, 0.4);
  }

  .demo-play-btn svg {
    width: 30px;
    height: 30px;
    fill: #fff;
    margin-left: 5px;
  }

  .demo-video-label {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  .demo-video-label strong { color: var(--color-text-secondary); }

  /* ── PROCESS SECTION ───────────────────────────────── */
  .demo-process-section {
    padding: 64px 0;
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }

  .demo-section-label {
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-text-muted);
    text-align: center;
    margin-bottom: 48px;
  }

  .demo-flow {
    display: flex;
    gap: 0;
    align-items: stretch;
  }

  .demo-step {
    flex: 1;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: 32px 24px;
    text-align: center;
    position: relative;
    background: var(--color-surface);
    transition: border-color var(--transition-base), transform var(--transition-base), box-shadow var(--transition-base);
  }

  .demo-step:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .demo-step + .demo-step {
    margin-left: 12px;
  }

  .demo-step--result {
    border-color: var(--color-accent);
    background: linear-gradient(135deg, rgba(200, 161, 90, 0.04) 0%, var(--color-surface) 100%);
  }

  .demo-step--result:hover {
    border-color: var(--color-gold-600);
  }

  .demo-step-num {
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 16px;
  }

  .demo-step--result .demo-step-num {
    color: var(--color-accent);
  }

  .demo-step-icon {
    font-size: 28px;
    margin-bottom: 14px;
    display: block;
  }

  .demo-step-title {
    font-family: var(--font-display);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    letter-spacing: -0.01em;
    margin-bottom: 10px;
  }

  .demo-step--result .demo-step-title {
    color: var(--color-gold-600);
  }

  .demo-step-desc {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    line-height: var(--line-height-relaxed);
  }

  .demo-step-arrow {
    position: absolute;
    right: -18px;
    top: 50%;
    transform: translateY(-50%);
    width: 24px;
    height: 24px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
    font-size: 10px;
    color: var(--color-text-muted);
  }

  /* ── PROOF STRIP ───────────────────────────────────── */
  .demo-proof-section {
    padding: 64px 0;
  }

  .demo-proof-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--color-border);
    border-radius: var(--radius-xl);
    overflow: hidden;
    border: 1px solid var(--color-border);
  }

  .demo-proof-cell {
    background: var(--color-surface);
    padding: 36px 24px;
    text-align: center;
  }

  .demo-proof-num {
    font-family: var(--font-display);
    font-size: 3rem;
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.04em;
    color: var(--color-text-primary);
    margin-bottom: 6px;
    line-height: 1;
  }

  .demo-proof-num em {
    font-style: normal;
    color: var(--color-primary);
  }

  .demo-proof-label {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    font-weight: var(--font-weight-medium);
  }

  /* ── GUARANTEE ─────────────────────────────────────── */
  .demo-guarantee-section {
    padding: 0 0 64px;
  }

  .demo-guarantee-box {
    background: linear-gradient(135deg, var(--color-ink-900) 0%, var(--color-ink-700) 100%);
    border-radius: var(--radius-xl);
    padding: 56px 48px;
    text-align: center;
  }

  .demo-guarantee-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-accent);
    background: rgba(200, 161, 90, 0.15);
    border: 1px solid rgba(200, 161, 90, 0.3);
    padding: 5px 16px;
    border-radius: var(--radius-full);
    margin-bottom: 24px;
  }

  .demo-guarantee-headline {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    font-weight: var(--font-weight-extrabold);
    letter-spacing: -0.02em;
    color: #fff;
    margin-bottom: 16px;
  }

  .demo-guarantee-sub {
    font-family: var(--font-sans);
    font-size: var(--font-size-base);
    color: rgba(255, 255, 255, 0.55);
    max-width: 560px;
    margin: 0 auto;
    line-height: var(--line-height-relaxed);
  }

  /* ── CTA FOOTER ────────────────────────────────────── */
  .demo-cta-section {
    padding: 64px 0 96px;
    text-align: center;
  }

  .demo-cta-note {
    font-family: var(--font-sans);
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin-top: 16px;
  }

  /* ── RESPONSIVE ────────────────────────────────────── */
  @media (max-width: 760px) {
    .demo-page-hero { padding: 56px 0 48px; }
    .demo-flow { flex-direction: column; gap: 12px; }
    .demo-step + .demo-step { margin-left: 0; }
    .demo-step-arrow { display: none; }
    .demo-proof-grid { grid-template-columns: 1fr; }
    .demo-guarantee-box { padding: 36px 24px; }
  }
</style>

<!-- HERO -->
<section class="demo-page-hero">
  <div class="container container--narrow">
    <span class="demo-eyebrow">Agency Demo</span>
    <h1 class="demo-headline">
      Here's how we book you<br>
      <em>3–5 qualified calls</em> in 30 days
    </h1>
    <p class="demo-sub">
      No retainer. No fluff. No SDR headcount.<br>
      Just meetings with the right people — guaranteed.
    </p>
    <div class="demo-cta-group">
      <%= link_to book_call_path, class: "cta-button cta-button--primary cta-button--large" do %>
        Book a 20-min call →
      <% end %>
      <%= link_to "#process", class: "cta-button cta-button--secondary" do %>
        ↓ See how it works
      <% end %>
    </div>
  </div>
</section>

<!-- VIDEO -->
<section class="demo-video-section">
  <div class="container container--narrow">
    <div class="demo-video-container">
      <div class="demo-video-placeholder">
        <div class="demo-play-btn">
          <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><polygon points="5,3 19,12 5,21" /></svg>
        </div>
        <p class="demo-video-label"><strong>90-second walkthrough</strong> — coming soon</p>
      </div>
    </div>
  </div>
</section>

<!-- PROCESS FLOW -->
<section class="demo-process-section" id="process">
  <div class="container">
    <p class="demo-section-label">The Sprint — how it works</p>
    <div class="demo-flow">

      <div class="demo-step">
        <div class="demo-step-num">Step 01</div>
        <span class="demo-step-icon">🎯</span>
        <div class="demo-step-title">Find your buyers</div>
        <div class="demo-step-desc">We nail your ICP. We find the exact trigger that makes someone ready to buy — a funding round, new hire, product launch.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step">
        <div class="demo-step-num">Step 02</div>
        <span class="demo-step-icon">🔍</span>
        <div class="demo-step-title">Enrich & qualify</div>
        <div class="demo-step-desc">We build a surgical 200–300 company list. Not spray-and-pray — every company matches a buying signal.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step">
        <div class="demo-step-num">Step 03</div>
        <span class="demo-step-icon">✍️</span>
        <div class="demo-step-title">Personalise at scale</div>
        <div class="demo-step-desc">We write messages personalised to each company. Two lines, specific, no fluff. You review and approve the first batch.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step">
        <div class="demo-step-num">Step 04</div>
        <span class="demo-step-icon">📤</span>
        <div class="demo-step-title">Send the sequence</div>
        <div class="demo-step-desc">We run the outreach across email and LinkedIn. You get notified on replies. We handle follow-ups.</div>
        <div class="demo-step-arrow">→</div>
      </div>

      <div class="demo-step demo-step--result">
        <div class="demo-step-num">Result</div>
        <span class="demo-step-icon">📅</span>
        <div class="demo-step-title">Booked calls</div>
        <div class="demo-step-desc">Not leads. Not "interested"s. Qualified calls on your calendar — with founders and heads of sales who fit your ICP.</div>
      </div>

    </div>
  </div>
</section>

<!-- PROOF STRIP -->
<section class="demo-proof-section">
  <div class="container container--narrow">
    <div class="demo-proof-grid">
      <div class="demo-proof-cell">
        <div class="demo-proof-num"><em>30</em></div>
        <div class="demo-proof-label">Day Sprint timeline</div>
      </div>
      <div class="demo-proof-cell">
        <div class="demo-proof-num">3–<em>5</em></div>
        <div class="demo-proof-label">Guaranteed qualified calls</div>
      </div>
      <div class="demo-proof-cell">
        <div class="demo-proof-num"><em>0</em></div>
        <div class="demo-proof-label">SDRs needed</div>
      </div>
    </div>
  </div>
</section>

<!-- GUARANTEE -->
<section class="demo-guarantee-section">
  <div class="container container--narrow">
    <div class="demo-guarantee-box">
      <span class="demo-guarantee-badge">Our guarantee</span>
      <h2 class="demo-guarantee-headline">If we don't hit 3 calls — we keep going.</h2>
      <p class="demo-guarantee-sub">
        The Sprint is a fixed-scope 30-day engagement. If we don't deliver minimum 3 qualified calls, we continue working at no additional cost until we do.
      </p>
    </div>
  </div>
</section>

<!-- CTA FOOTER -->
<section class="demo-cta-section">
  <div class="container container--narrow">
    <h2 style="font-family: var(--font-display); font-size: clamp(1.5rem, 3vw, 2rem); font-weight: var(--font-weight-extrabold); letter-spacing: -0.02em; color: var(--color-text-primary); margin-bottom: 12px;">
      Ready to see if this works for you?
    </h2>
    <p style="font-family: var(--font-sans); color: var(--color-text-muted); margin-bottom: 32px;">
      Book a 20-min call. No pitch. I'll tell you exactly what ICP we'd target and you decide if it's worth doing.
    </p>
    <%= link_to book_call_path, class: "cta-button cta-button--primary cta-button--large" do %>
      Book a 20-min call →
    <% end %>
    <p class="demo-cta-note">No commitment. Just a conversation.</p>
  </div>
</section>
