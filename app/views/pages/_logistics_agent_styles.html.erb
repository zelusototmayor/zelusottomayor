<style>
  /* ── LOGISTICS AGENT ONE-PAGER ─────────────────────────────
     All selectors scoped to body.logistics-agent-page to avoid
     clashing with global .container / .btn / heading styles.
     ────────────────────────────────────────────────────────── */

  body.logistics-agent-page {
    --lga-ink-900: #0b1220;
    --lga-ink-800: #121a2b;
    --lga-ink-700: #1b2538;
    --lga-ink-600: #27324a;
    --lga-ink-500: #3b4863;
    --lga-ink-400: #5d6a82;
    --lga-ink-300: #8692a8;
    --lga-ink-200: #c1c8d4;
    --lga-ink-100: #e6e9ef;
    --lga-ink-50: #f5f6f9;
    --lga-sand-50: #fbfaf7;
    --lga-sand-100: #f4f0e8;
    --lga-sand-200: #e7dfd1;
    --lga-gold-500: #c8a15a;
    --lga-gold-600: #b8893d;
    --lga-forest-600: #275a4e;
    --lga-forest-500: #2f6f5e;
    --lga-forest-400: #3c826f;
    --lga-red-600: #c0392b;
    --lga-primary: var(--lga-forest-600);
    --lga-accent: var(--lga-gold-500);
    --lga-font-display: 'Fraunces', serif;
    --lga-font-sans: 'Manrope', "Helvetica Neue", Arial, sans-serif;
    --lga-font-mono: 'IBM Plex Mono', "SF Mono", Monaco, monospace;
    --lga-gradient-hero:
      radial-gradient(circle at 20% 20%, rgba(200, 161, 90, 0.16), transparent 45%),
      radial-gradient(circle at 80% 0%, rgba(39, 90, 78, 0.18), transparent 45%),
      linear-gradient(180deg, var(--lga-sand-50) 0%, var(--lga-sand-100) 100%);
    --lga-gradient-text: linear-gradient(120deg, var(--lga-ink-900) 0%, var(--lga-forest-600) 55%, var(--lga-gold-500) 100%);
    --lga-gradient-dark: linear-gradient(135deg, var(--lga-ink-900) 0%, var(--lga-ink-700) 100%);
    --lga-shadow-sm: 0 2px 8px rgba(11, 18, 32, 0.06);
    --lga-shadow-md: 0 8px 24px rgba(11, 18, 32, 0.08);
    --lga-shadow-lg: 0 20px 48px rgba(11, 18, 32, 0.12);
    --lga-radius-sm: 6px;
    --lga-radius-md: 10px;
    --lga-radius-lg: 16px;
    --lga-radius-xl: 22px;

    font-family: var(--lga-font-sans);
    font-weight: 400;
    color: var(--lga-ink-900);
    background: var(--lga-sand-50);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body.logistics-agent-page .lga-wrap { scroll-behavior: smooth; }

  body.logistics-agent-page .lga-wrap,
  body.logistics-agent-page .lga-wrap *,
  body.logistics-agent-page .lga-wrap *::before,
  body.logistics-agent-page .lga-wrap *::after {
    box-sizing: border-box;
  }

  body.logistics-agent-page .lga-wrap .lga-container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 28px;
  }

  body.logistics-agent-page .lga-wrap .lga-container--narrow {
    max-width: 820px;
    margin: 0 auto;
    padding: 0 28px;
  }

  body.logistics-agent-page .lga-wrap h1,
  body.logistics-agent-page .lga-wrap h2,
  body.logistics-agent-page .lga-wrap h3,
  body.logistics-agent-page .lga-wrap h4 {
    font-family: var(--lga-font-display);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
    color: var(--lga-ink-900);
    margin: 0;
  }

  body.logistics-agent-page .lga-wrap h2 {
    font-size: clamp(1.8rem, 3.6vw, 2.6rem);
    margin-bottom: 14px;
  }
  body.logistics-agent-page .lga-wrap h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 10px;
  }

  body.logistics-agent-page .lga-wrap p {
    color: var(--lga-ink-600);
    font-size: 1rem;
    margin: 0;
  }

  body.logistics-agent-page .lga-wrap a {
    color: var(--lga-primary);
    text-decoration: none;
    transition: color 0.15s ease;
  }
  body.logistics-agent-page .lga-wrap a:hover { color: var(--lga-forest-500); }

  /* ───── TOPBAR ───── */
  body.logistics-agent-page .lga-wrap .lga-topbar {
    background: var(--lga-ink-900);
    color: var(--lga-ink-200);
    padding: 10px 0;
    font-family: var(--lga-font-mono);
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-align: center;
    text-transform: uppercase;
  }
  body.logistics-agent-page .lga-wrap .lga-topbar span { color: var(--lga-gold-500); }

  /* ───── HERO ───── */
  body.logistics-agent-page .lga-wrap .lga-hero {
    padding: 90px 0 70px;
    background: var(--lga-gradient-hero);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  body.logistics-agent-page .lga-wrap .lga-eyebrow {
    display: inline-block;
    font-family: var(--lga-font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--lga-primary);
    background: rgba(39, 90, 78, 0.08);
    border: 1px solid rgba(39, 90, 78, 0.2);
    padding: 6px 16px;
    border-radius: 999px;
    margin-bottom: 26px;
  }

  body.logistics-agent-page .lga-wrap .lga-hero h1 {
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    max-width: 820px;
    margin: 0 auto 18px;
    letter-spacing: -0.03em;
  }
  body.logistics-agent-page .lga-wrap .lga-hero h1 em {
    font-style: normal;
    background: var(--lga-gradient-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  body.logistics-agent-page .lga-wrap .lga-hero__sub {
    font-size: 1.12rem;
    max-width: 620px;
    margin: 0 auto 30px;
    color: var(--lga-ink-500);
    line-height: 1.65;
  }

  body.logistics-agent-page .lga-wrap .lga-hero__metrics {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    max-width: 720px;
    margin: 0 auto;
  }
  body.logistics-agent-page .lga-wrap .lga-metric {
    background: #ffffff;
    border: 1px solid var(--lga-ink-100);
    border-radius: var(--lga-radius-md);
    padding: 20px 14px;
    box-shadow: var(--lga-shadow-sm);
  }
  body.logistics-agent-page .lga-wrap .lga-metric__value {
    font-family: var(--lga-font-display);
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 800;
    color: var(--lga-primary);
    line-height: 1;
    margin-bottom: 6px;
  }
  body.logistics-agent-page .lga-wrap .lga-metric__label {
    font-family: var(--lga-font-mono);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--lga-ink-400);
  }

  /* ───── SECTIONS ───── */
  body.logistics-agent-page .lga-wrap section { padding: 70px 0; }
  body.logistics-agent-page .lga-wrap section.lga-alt {
    background: #ffffff;
    border-top: 1px solid var(--lga-ink-100);
    border-bottom: 1px solid var(--lga-ink-100);
  }

  body.logistics-agent-page .lga-wrap .lga-section-header {
    text-align: center;
    max-width: 720px;
    margin: 0 auto 48px;
  }
  body.logistics-agent-page .lga-wrap .lga-section-header p {
    margin-top: 10px;
    font-size: 1.05rem;
    color: var(--lga-ink-500);
  }

  /* ───── PROBLEM ───── */
  body.logistics-agent-page .lga-wrap .lga-problem-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
  body.logistics-agent-page .lga-wrap .lga-problem {
    background: #ffffff;
    border: 1px solid var(--lga-ink-100);
    border-radius: var(--lga-radius-md);
    padding: 26px 22px;
  }
  body.logistics-agent-page .lga-wrap .lga-problem__num {
    font-family: var(--lga-font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--lga-gold-600);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 10px;
  }
  body.logistics-agent-page .lga-wrap .lga-problem__title {
    font-family: var(--lga-font-display);
    font-size: 1.15rem;
    font-weight: 700;
    color: var(--lga-ink-900);
    margin-bottom: 8px;
  }
  body.logistics-agent-page .lga-wrap .lga-problem__desc {
    font-size: 0.95rem;
    color: var(--lga-ink-500);
    line-height: 1.6;
  }

  /* ───── MODES ───── */
  body.logistics-agent-page .lga-wrap .lga-modes {
    background: var(--lga-gradient-dark);
    color: var(--lga-ink-100);
  }
  body.logistics-agent-page .lga-wrap .lga-modes h2 { color: #ffffff; }
  body.logistics-agent-page .lga-wrap .lga-modes .lga-section-header p { color: var(--lga-ink-300); }
  body.logistics-agent-page .lga-wrap .lga-modes-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 28px;
  }
  body.logistics-agent-page .lga-wrap .lga-mode {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--lga-radius-lg);
    padding: 36px 30px;
    transition: all 0.2s ease;
  }
  body.logistics-agent-page .lga-wrap .lga-mode:hover {
    border-color: var(--lga-gold-500);
    background: rgba(255, 255, 255, 0.06);
  }
  body.logistics-agent-page .lga-wrap .lga-mode__tag {
    font-family: var(--lga-font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--lga-gold-500);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 14px;
  }
  body.logistics-agent-page .lga-wrap .lga-mode__title {
    font-family: var(--lga-font-display);
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 14px;
  }
  body.logistics-agent-page .lga-wrap .lga-mode__desc {
    font-size: 1rem;
    color: var(--lga-ink-200);
    line-height: 1.65;
    margin-bottom: 22px;
  }
  body.logistics-agent-page .lga-wrap .lga-mode__examples {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  body.logistics-agent-page .lga-wrap .lga-mode__examples li {
    font-size: 0.9rem;
    color: var(--lga-ink-200);
    padding: 8px 0 8px 22px;
    position: relative;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }
  body.logistics-agent-page .lga-wrap .lga-mode__examples li:first-child { border-top: none; }
  body.logistics-agent-page .lga-wrap .lga-mode__examples li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--lga-gold-500);
    font-weight: 600;
  }
  body.logistics-agent-page .lga-wrap .lga-mode__status {
    margin-top: 18px;
    display: inline-block;
    font-family: var(--lga-font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 5px 10px;
    border-radius: 4px;
  }
  body.logistics-agent-page .lga-wrap .lga-status--live {
    background: rgba(47, 111, 94, 0.25);
    color: var(--lga-forest-400);
    border: 1px solid var(--lga-forest-500);
  }
  body.logistics-agent-page .lga-wrap .lga-status--extension {
    background: rgba(200, 161, 90, 0.2);
    color: var(--lga-gold-500);
    border: 1px solid var(--lga-gold-600);
  }

  /* ───── FLOW ───── */
  body.logistics-agent-page .lga-wrap .lga-flow {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    position: relative;
  }
  body.logistics-agent-page .lga-wrap .lga-flow-step {
    background: #ffffff;
    border: 1px solid var(--lga-ink-100);
    border-radius: var(--lga-radius-md);
    padding: 22px 18px;
    text-align: center;
    position: relative;
  }
  body.logistics-agent-page .lga-wrap .lga-flow-step__num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: var(--lga-primary);
    color: #ffffff;
    border-radius: 50%;
    font-family: var(--lga-font-display);
    font-weight: 700;
    font-size: 1rem;
    margin-bottom: 12px;
  }
  body.logistics-agent-page .lga-wrap .lga-flow-step__title {
    font-family: var(--lga-font-display);
    font-weight: 700;
    font-size: 1rem;
    margin-bottom: 6px;
    color: var(--lga-ink-900);
  }
  body.logistics-agent-page .lga-wrap .lga-flow-step__desc {
    font-size: 0.85rem;
    color: var(--lga-ink-500);
    line-height: 1.5;
  }
  body.logistics-agent-page .lga-wrap .lga-flow-step__time {
    margin-top: 10px;
    font-family: var(--lga-font-mono);
    font-size: 0.7rem;
    color: var(--lga-gold-600);
    letter-spacing: 0.08em;
  }

  /* ───── CONTROL ───── */
  body.logistics-agent-page .lga-wrap .lga-control-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    align-items: start;
  }
  body.logistics-agent-page .lga-wrap .lga-control-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  body.logistics-agent-page .lga-wrap .lga-control-list li {
    padding: 14px 0 14px 32px;
    position: relative;
    border-bottom: 1px solid var(--lga-ink-100);
    font-size: 0.98rem;
    color: var(--lga-ink-700);
  }
  body.logistics-agent-page .lga-wrap .lga-control-list li:last-child { border-bottom: none; }
  body.logistics-agent-page .lga-wrap .lga-control-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    top: 14px;
    width: 20px;
    height: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--lga-forest-500);
    color: #ffffff;
    border-radius: 50%;
    font-size: 0.7rem;
    font-weight: 700;
  }
  body.logistics-agent-page .lga-wrap .lga-control-list li strong {
    color: var(--lga-ink-900);
    font-weight: 600;
  }

  body.logistics-agent-page .lga-wrap .lga-escalation-card {
    background: var(--lga-sand-100);
    border-left: 4px solid var(--lga-gold-500);
    border-radius: var(--lga-radius-md);
    padding: 28px;
  }
  body.logistics-agent-page .lga-wrap .lga-escalation-card h3 {
    font-size: 1.1rem;
    margin-bottom: 12px;
    color: var(--lga-ink-900);
  }
  body.logistics-agent-page .lga-wrap .lga-escalation-card p {
    font-size: 0.95rem;
    line-height: 1.65;
  }

  /* ───── PRIVACY ───── */
  body.logistics-agent-page .lga-wrap .lga-privacy { background: var(--lga-sand-100); }
  body.logistics-agent-page .lga-wrap .lga-privacy-col {
    max-width: 640px;
    margin: 0 auto;
    background: #ffffff;
    border: 1px solid var(--lga-ink-100);
    border-radius: var(--lga-radius-md);
    padding: 32px 36px;
    box-shadow: var(--lga-shadow-sm);
  }
  body.logistics-agent-page .lga-wrap .lga-privacy-col h3 {
    font-family: var(--lga-font-display);
    font-size: 1.15rem;
    margin-bottom: 14px;
    color: var(--lga-ink-900);
    padding-bottom: 10px;
    border-bottom: 2px solid var(--lga-ink-200);
  }
  body.logistics-agent-page .lga-wrap .lga-privacy-col ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  body.logistics-agent-page .lga-wrap .lga-privacy-col li {
    padding: 10px 0 10px 24px;
    position: relative;
    font-size: 0.94rem;
    color: var(--lga-ink-700);
    line-height: 1.55;
  }
  body.logistics-agent-page .lga-wrap .lga-privacy-col li::before {
    content: "•";
    position: absolute;
    left: 6px;
    color: var(--lga-primary);
    font-weight: 700;
  }

  /* ───── TIMELINE ───── */
  body.logistics-agent-page .lga-wrap .lga-timeline {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    max-width: 720px;
    margin: 0 auto;
  }
  body.logistics-agent-page .lga-wrap .lga-timeline-row {
    display: grid;
    grid-template-columns: 120px 1fr;
    gap: 24px;
    padding: 18px 22px;
    background: #ffffff;
    border: 1px solid var(--lga-ink-100);
    border-radius: var(--lga-radius-md);
    align-items: start;
  }
  body.logistics-agent-page .lga-wrap .lga-timeline-row__phase {
    font-family: var(--lga-font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--lga-gold-600);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  body.logistics-agent-page .lga-wrap .lga-timeline-row__content h4 {
    font-family: var(--lga-font-display);
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--lga-ink-900);
    margin-bottom: 4px;
  }
  body.logistics-agent-page .lga-wrap .lga-timeline-row__content p {
    font-size: 0.9rem;
    color: var(--lga-ink-500);
    line-height: 1.55;
  }

  /* ───── CTA ───── */
  body.logistics-agent-page .lga-wrap .lga-cta {
    background: var(--lga-gradient-dark);
    color: #ffffff;
    text-align: center;
  }
  body.logistics-agent-page .lga-wrap .lga-cta h2 { color: #ffffff; }
  body.logistics-agent-page .lga-wrap .lga-cta p {
    color: var(--lga-ink-200);
    font-size: 1.08rem;
    max-width: 560px;
    margin: 14px auto 28px;
  }
  body.logistics-agent-page .lga-wrap .lga-cta__actions {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
  }
  body.logistics-agent-page .lga-wrap .lga-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 28px;
    border-radius: var(--lga-radius-md);
    font-family: var(--lga-font-sans);
    font-weight: 600;
    font-size: 0.98rem;
    letter-spacing: 0.01em;
    text-decoration: none;
    transition: all 0.15s ease;
    border: 2px solid transparent;
    cursor: pointer;
  }
  body.logistics-agent-page .lga-wrap .lga-btn--primary {
    background: var(--lga-gold-500);
    color: var(--lga-ink-900);
    border-color: var(--lga-gold-500);
  }
  body.logistics-agent-page .lga-wrap .lga-btn--primary:hover {
    background: var(--lga-gold-600);
    border-color: var(--lga-gold-600);
    color: var(--lga-ink-900);
    transform: translateY(-1px);
  }
  body.logistics-agent-page .lga-wrap .lga-btn--secondary {
    background: transparent;
    color: #ffffff;
    border-color: rgba(255, 255, 255, 0.3);
  }
  body.logistics-agent-page .lga-wrap .lga-btn--secondary:hover {
    border-color: #ffffff;
    background: rgba(255, 255, 255, 0.08);
    color: #ffffff;
  }

  /* ───── RESPONSIVE ───── */
  @media (max-width: 860px) {
    body.logistics-agent-page .lga-wrap .lga-hero { padding: 60px 0 50px; }
    body.logistics-agent-page .lga-wrap .lga-hero__metrics { grid-template-columns: 1fr; gap: 12px; }
    body.logistics-agent-page .lga-wrap .lga-problem-grid { grid-template-columns: 1fr; }
    body.logistics-agent-page .lga-wrap .lga-modes-grid { grid-template-columns: 1fr; }
    body.logistics-agent-page .lga-wrap .lga-flow { grid-template-columns: 1fr 1fr; }
    body.logistics-agent-page .lga-wrap .lga-control-grid { grid-template-columns: 1fr; }
    body.logistics-agent-page .lga-wrap .lga-timeline-row { grid-template-columns: 1fr; gap: 6px; }
    body.logistics-agent-page .lga-wrap section { padding: 50px 0; }
  }
  @media (max-width: 500px) {
    body.logistics-agent-page .lga-wrap .lga-flow { grid-template-columns: 1fr; }
  }
</style>
