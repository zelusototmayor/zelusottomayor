<main class="min-h-screen">
  <!-- Hero Section -->
  <section class="hero-section">
    <div class="hero-content">
      <h1 class="hero-title">
        Building <span class="hero-highlight">experiences</span><br>
        for myself, for you to enjoy
      </h1>
      <p class="hero-subtitle">Welcome to my project showcase</p>
    </div>
  </section>

  <!-- Projects Section -->
  <section class="projects-section">
    <div class="projects-container">
      <% if @projects.any? %>
        <div class="projects-grid">
          <% @projects.each do |project| %>
            <div class="project-folder">
              <div class="project-icon">
                <% if project[:icon].present? %>
                  <%= image_tag project[:icon], alt: project[:name], class: "project-icon-img" %>
                <% else %>
                  📁
                <% end %>
              </div>
              <div class="project-content">
                <h3 class="project-title">
                  <%= project[:name] %>
                </h3>
                <p class="project-description"><%= project[:description] %></p>
                <% if project[:link].present? %>
                  <a href="<%= project[:link] %>" target="_blank" rel="noopener" class="external-link">
                    View Project <span class="external-icon">↗</span>
                  </a>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="no-projects">
          <div class="empty-folder">📁</div>
          <p>No projects yet. Check back soon!</p>
          <%= link_to "View All Projects", projects_path, class: "projects-link" %>
        </div>
      <% end %>
    </div>
  </section>

  <!-- FAQ Section for SEO -->
  <section class="faq-section py-20 px-4 bg-slate-50">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-3xl font-bold mb-10 text-center">AI Automation & Development FAQ</h2>
      <div class="space-y-6">
        <article class="p-6 bg-white rounded-xl shadow-sm border border-slate-100">
          <h3 class="text-xl font-semibold mb-3">What does an AI automation consultant do?</h3>
          <p class="text-slate-600">As an AI automation consultant, I help businesses identify manual workflows that can be optimized using LLMs and custom AI agents. This includes everything from automated lead generation to intelligent content processing and customer support systems.</p>
        </article>

        <article class="p-6 bg-white rounded-xl shadow-sm border border-slate-100">
          <h3 class="text-xl font-semibold mb-3">Why hire a Rails AI automation developer in Lisbon?</h3>
          <p class="text-slate-600">Lisbon is a thriving tech hub, and combining Ruby on Rails with modern AI integrations allows for rapid, secure, and scalable automation solutions. Working with a local expert ensures clear communication and deep understanding of the European tech landscape while serving global clients.</p>
        </article>

        <article class="p-6 bg-white rounded-xl shadow-sm border border-slate-100">
          <h3 class="text-xl font-semibold mb-3">How can AI workflow automation help my agency?</h3>
          <p class="text-slate-600">For agencies, AI workflow automation reduces the time spent on repetitive tasks like data entry, initial research, and content drafting. This allows your team to focus on high-level strategy and creative work, significantly increasing your agency's capacity and margins.</p>
        </article>
      </div>
    </div>
  </section>
</main>
